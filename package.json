{
  "name": "playwright-ts-sdet-framework",
  "version": "0.1.0",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "lint": "eslint . --ext .ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^24.10.1",
    "eslint": "^8.0.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
